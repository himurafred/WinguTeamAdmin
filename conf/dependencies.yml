# Application dependencies

require:
    - play
    - customModules -> Authenticate

repositories:

    - playCustomModules:
        type: local
        artifact: "/home/himurafred/application_mobile/play/Authenticate"
        contains:
            - customModules -> *
